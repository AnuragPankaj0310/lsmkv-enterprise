{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm ci",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://REPLACE_WITH_RAILWAY_API_URL/:path*"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "env": {
    "VITE_API_URL": {
      "description": "Base URL of your Railway/Render API deployment (e.g. https://mykvstore-api.up.railway.app). Leave empty when using Railway full-stack (Nginx handles routing).",
      "required": false
    }
  }
}
